name: Test CI Job
on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build Static Pages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v1
        with:
          node-version: 10.x
      - name: npm install
        run: |
          npm install
          npm run generate